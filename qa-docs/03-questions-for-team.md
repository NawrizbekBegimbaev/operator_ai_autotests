# 03. Вопросы к команде

**Назначение файла:** сюда собираются все места, где поведение системы непонятно или где документация расходится с кодом настолько, что QA не может сам решить — это задумано так или это дефект. Каждый вопрос имеет статус и место для ответа.

**Как пользоваться:** вопрос задаётся продакту или разработчику → ответ записывается в поле «Ответ» → статус меняется на «Отвечен» → из ответа рождается либо баг-репорт, либо тест-кейс, либо строчка в документации.

> ### 👥 Для кого этот файл, а для кого — другой
>
> **Этот файл — рабочий.** Здесь код, названия файлов, номера строк, технические подробности. Он для разработчиков и для меня.
>
> **Для встречи с продактом, руководителем или заказчиком есть `07-questions-simple.md`** — те же вопросы, но обычным языком, с бытовыми аналогиями и без единого технического термина. Его можно открыть на созвоне и идти по пунктам, ничего не объясняя дополнительно.
>
> **Разница между файлами:**
>
> | | Этот файл (`03`) | Простой файл (`07`) |
> | --- | --- | --- |
> | Что содержит | **Все** вопросы, включая отвеченные и снятые | **Только открытые** |
> | Отвеченные вопросы | Остаются с заполненным ответом — это история решений | Удаляются сразу после получения ответа |
> | Порядок | По номерам Q-01 … Q-16 | По срочности, самое важное сверху |
>
> **Соответствие номеров на 25.07:**
>
> | № в простом файле | Здесь |
> | --- | --- |
> | 1 | Q-10 |
> | 2 | Q-14 |
> | 3 | Q-11 |
> | 4 | Q-03 |
> | 5 | Q-06 |
> | 6 | Q-07 |
> | 7 | Q-08 |
> | 8 | Q-16 |
> | 9 | Q-01 |
> | 10 | Q-12 |
>
> ⚠️ **Номера в простом файле не фиксированы** — они меняются, когда вопросы закрываются. Постоянный идентификатор — только Q-XX из этого файла. При обсуждении лучше ссылаться на него.
>
> **Оба файла надо обновлять вместе.**

**Статусы:** 🔴 Открыт · 🟡 Задан, ждём ответа · 🟢 Отвечен · ⚪️ Снят (оказался не вопросом)

> ### 📌 Обновление от 25.07.2026
>
> Команда передала пять новых документов: `MVP_SCOPE_UPDATED_2026-07-24.md` и четыре журнала работ. В скоупе зафиксированы решения продакта от 24.07 — они **закрывают четыре наших вопроса** (Q-02, Q-04, Q-05, Q-09) и меняют формулировку двух других (Q-03, Q-11).
>
> Ответы записаны в соответствующие разделы с пометкой «получено из скоупа», а не из личного разговора. Это важное различие: формулировки взяты из таблиц документа, и по некоторым нужна очная сверка — там, где это так, отмечено отдельно.
>
> Добавлены четыре новых вопроса: **Q-13** … **Q-16**. В конце файла — новое приложение со списком вопросов, которые **команда уже задала клиенту и разработчикам сама**, чтобы мы их не дублировали.

---

## Сводка

| № | Вопрос | Кому | Важность | Статус |
| --- | --- | --- | --- | --- |
| [Q-01](#q-01) | ROP может сам управлять подключением к AmoCRM в обход интерфейса | Продакт + разработчик BE | 🔥 Высокая | 🟢 **Отвечен 25.07** — не должен. Подтверждён дефект, кандидат BUG-014 (P1) |
| [Q-02](#q-02) | Что должно происходить с лидом после исчерпания попыток дозвона (статус «Sifatsiz») | Продакт | 🔥 Высокая | 🟢 **Отвечен 24.07** |
| [Q-03](#q-03) | Должен ли статус меняться сам, если AI не заполнил обязательные поля | Продакт | Средняя ↓ | 🟢 **Отвечен 25.07** — да, «Chala gaplashdi». Кандидат в BUG-010 |
| [Q-04](#q-04) | Что должно происходить при переносе времени визита клиентом | Продакт | 🔥 Высокая | 🟢 **Отвечен 24.07** |
| [Q-05](#q-05) | Что происходит с лидом после визита в центр («оплатил / не оплатил») | Продакт | Средняя | 🟢 **Отвечен 24.07** |
| [Q-06](#q-06) | Что такое «Yakuniy holat» и как система должна понимать, что работа закончена | Продакт | Средняя ↑ | 🟢 **Отвечен 25.07** — финиш только 142; из 143 лид возвращается. Кандидат BUG-013 |
| [Q-07](#q-07) | Приемлемы ли ограничения автоперевода «Uchrashuv» → «Bugun keladi» | Продакт + BE | Средняя ↑ | 🟢 **Отвечен 25.07** — два кандидата в баги |
| [Q-08](#q-08) | Каким должно быть поведение кнопки «пропустить лида» | Продакт | Средняя | 🟡 **Отвечен 25.07** — не удаление, а лимит 2–3 в день. Число не зафиксировано |
| [Q-09](#q-09) | Требование ли — звонить клиентам только с определённого номера | Продакт | Низкая | 🟢 **Отвечен 24.07** |
| [Q-10](#q-10) | **Контракт с AmoCRM:** где зафиксированы ключевые слова статусов и названия полей, кто отвечает за их неизменность | Продакт + BE + внедрение | 🔥🔥 Критическая | 🟢 **Отвечен 25.07** — контракт не нужен, расхождение = баг |
| [Q-11](#q-11) | Два параметра «Mezonlar» сохраняются, но не влияют на работу очереди — статус функциональности | Продакт + BE | 🔥 Высокая | 🟢 **Отвечен 25.07** |
| [Q-12](#q-12) | Новый модуль тарифов: ограничивает ли тариф доступ к функциям, или это пока витрина | Продакт + BE | 🔥 Высокая | 🟡 **Отвечен 25.07** — ограничивает, реализовано. Матрица выведена самостоятельно |
| [Q-13](#q-13) | **Какой документ скоупа считать действующим** и где два недостающих документа решений | Продакт | 🔥 Высокая | 🟢 **Отвечен 25.07** (частично) |
| [Q-14](#q-14) | Политика попыток: что значит «переносятся на следующие дни» и сбрасывается ли счётчик при ручной правке статуса | Продакт + BE | 🔥 Высокая | 🟢 **Отвечен 25.07** |
| [Q-15](#q-15) | **Доступ к тестовому стенду** и сроки закрытия внешних блокеров | Разработчики + заказчик | 🔥🔥 Критическая | 🟢 **Отвечен 25.07** (частично) |
| [Q-16](#q-16) | Автозакрытие смены по бездействию — чему равно N минут | Продакт | Низкая | 🟡 **Отвечен 25.07** — механизм заменён на автопаузу, реализация в будущем |

> ### ✅ Обновление 25.07 — снят главный блокер
>
> Получены ответы по трём срочным пунктам:
>
> 1. **Доступ к окружениям есть** — и к staging, и к production (Q-15). Ограничение «проверять негде» снято;
> 2. **Ключ Gemini ротирован** — BUG-009 переходит в статус «Исправлен», закрываем после самостоятельной проверки;
> 3. **Действует новый скоуп** от 24.07 (Q-13). Старый `MVP-Scope.md` для оценки готовности не используем.
>
> **Оба вопроса помечены «отвечен частично»** — закрыта главная часть, но остались хвосты: по Q-15 не подтверждены тестовые аккаунты AmoCRM и OnlinePBX и три внешних блокера (лимит биллинга, деплой, extension), по Q-13 не переданы два документа с решениями.
>
> ⚠️ **Доступ к production не отменяет правила проекта:** функциональные проверки — только на staging. На проде допустимо пассивное наблюдение без изменения данных.

---

<a id="q-01"></a>
## Q-01. Должен ли ROP управлять подключением к AmoCRM самостоятельно?

**Кому:** продакт (вопрос про замысел) + разработчик бэкенда (вопрос про реализацию)
**Важность:** высокая — затрагивает безопасность и разграничение прав
**Статус:** 🟡 Задан, ждём ответа
**Дата обнаружения:** 21.07.2026

### Суть простыми словами

Подключение к AmoCRM — это набор ключей доступа к CRM клиента (домен, client_id, client_secret, токены доступа). По документации этим занимается Super-admin: он заводит клиента и настраивает ему интеграции.

В интерфейсе так и есть — кнопка настройки AmoCRM показывается **только** Super-админу.

Но интерфейс — это лишь «витрина». Под ней есть API, к которому можно обратиться напрямую, минуя кнопки. И вот там ROP может делать со своим подключением к AmoCRM **всё, что угодно**: создать, посмотреть (включая секретные ключи), изменить и удалить.

Аналогия: на двери кабинета нет ручки, но дверь не заперта — достаточно толкнуть.

### Что говорит документация

**ТЗ, раздел 3.5 «Permission Matrix»:**

| Функция | Super Admin | ROP | Operator |
| --- | --- | --- | --- |
| Configure Integrations | ✅ | ❌ | ❌ |

ТЗ (раздел 3.2) относит настройку интеграций AmoCRM и OnlinePBX к зоне ответственности Super-admin. В обязанностях ROP (раздел 3.3) настройки интеграций **нет вообще**.

**MVP-Scope, раздел 2 «Admin (Super-admin)»:** строка «AmoCRM connection per ROP (CRUD) — In MVP — Done/Done» — то есть фича числится за админской частью.

### Что говорит код

**Бэкенд — доступ ROP открыт полностью.**

Проверка доступа к конфигу AmoCRM (`project/back/internal/usecase/amocrm/service.go:3153`):

```go
func canAccessConfig(principal entity.Principal, config entity.AmoCRMConfig) bool {
	switch principal.Role {
	case entity.RoleSuperAdmin:
		return true
	case entity.RoleROP:
		return config.ROPID == principal.UserID   // ← ROP допускается к своему конфигу
	default:
		return false
	}
}
```

При создании конфига (`service.go:1573`, функция `resolveConfigROPID`) для роли ROP код **молча подставляет его собственный id** — то есть запрос проходит без ошибки:

```go
case entity.RoleROP:
	return principal.UserID, nil
```

Итог по эндпоинтам:

| Эндпоинт | Super-admin | ROP | Что реально может ROP |
| --- | --- | --- | --- |
| `POST /v1/amocrm/configs` | ✅ | ✅ | Создать себе подключение к любому домену AmoCRM |
| `GET /v1/amocrm/configs` | ✅ | ✅ | Увидеть свои конфиги |
| `GET /v1/amocrm/configs/{id}` | ✅ | ✅ | Прочитать конфиг — **включая `client_secret` и токены доступа**, если они отдаются в ответе |
| `PATCH /v1/amocrm/configs/{id}` | ✅ | ✅ | Подменить домен, ключи, токены, признак активности |
| `DELETE /v1/amocrm/configs/{id}` | ✅ | ✅ | Удалить подключение и обезглавить свою интеграцию |

**Фронтенд — доступ ROP закрыт.**

В `project/admin/src/sections/users/user-list-view.tsx` кнопка появляется только при условии:

```
canManageAmoCrm = currentRole === 'superadmin' && targetRole === 'rop'
```

То есть **защита реализована только на уровне интерфейса**, а на уровне API её нет. Это классическая ситуация «спрятали кнопку вместо того, чтобы закрыть доступ».

### Главный аргумент, что это дефект, а не замысел

Соседний модуль — **подключение OnlinePBX — закрыт правильно.** В `project/back/internal/usecase/onlinepbx/service.go` у каждого метода работы с конфигом стоит явная проверка (строки 479, 510, 707, 730, 742, 752, 778):

```go
if principal.Role != entity.RoleSuperAdmin {
	return ErrForbidden
}
```

Две интеграции, одна и та же задача, **разный уровень защиты**. Похоже не на осознанное решение, а на пропущенную проверку в модуле AmoCRM.

Причина, по которой это вообще стало возможным: в проекте **нет единого механизма проверки прав**. Роль проверяется вручную внутри каждого метода бизнес-логики. Один забытый `if` — и эндпоинт открыт. Это системная проблема, Q-01 — её первое найденное проявление.

### Чем это грозит, если это дефект

1. **Подмена CRM.** ROP может переключить своё подключение на чужой или подставной аккаунт AmoCRM. Куда после этого уедут данные клиентов и откуда приедут лиды — вопрос открытый.
2. **Утечка ключей.** Если `client_secret` и токены доступа возвращаются в ответе `GET`, ROP получает полный доступ к CRM своей компании в обход Operator AI — то есть может делать с CRM что угодно от имени интеграции. Нужно отдельно проверить, маскируются ли секреты в ответе (пункт 2 плана проверки).
3. **Самострел.** ROP может случайно удалить подключение и остановить работу всего своего отдела — при этом Super-admin об этом не узнает.
4. **Расхождение с ТЗ.** Матрица прав в ТЗ утверждает обратное. Пока вопрос не решён, **непонятно, какой результат считать правильным при тестировании** — а значит, нельзя написать ни один тест на права доступа к этому модулю.

### Вопросы, на которые нужен ответ

1. **Задумано ли, что ROP управляет своим подключением к AmoCRM самостоятельно?**
   - Если **да** — нужно обновить ТЗ (матрицу прав в разделе 3.5) и объяснить, почему для OnlinePBX правило другое. И тогда отдельно решить: показывать ли ROP кнопку в интерфейсе, или он должен пользоваться API.
   - Если **нет** — это дефект прав доступа, заводим баг: добавить проверку `RoleSuperAdmin` в методы работы с конфигом AmoCRM, по образцу модуля OnlinePBX.
2. **Должен ли ROP вообще видеть секреты (`client_secret`, `access_token`, `refresh_token`) своего подключения?** Даже если управление ему разрешено — отдавать секреты в открытом виде необязательно.
3. **Планируется ли сделать единый механизм проверки прав** вместо ручных проверок в каждом методе? От этого зависит объём тестов на права доступа: при ручных проверках нужно тестировать **каждый эндпоинт отдельно**, автоматически ничего не гарантировано.

### Как проверить (план для QA после ответа)

1. Войти под ROP → получить токен доступа.
2. Отправить напрямую `GET /v1/amocrm/configs` с этим токеном → зафиксировать, приходят ли секреты в открытом виде.
3. Отправить `PATCH /v1/amocrm/configs/{id}` со сменой домена → зафиксировать код ответа. Ожидаемый по ТЗ — `403 Forbidden`. Фактический по коду — `200 OK`.
4. Отправить `DELETE /v1/amocrm/configs/{id}` → зафиксировать код ответа.
5. Для сравнения повторить то же самое с `/v1/onlinepbx/configs` → там ожидается `403` (эталонное поведение).
6. Проверить, может ли ROP подставить в запрос **чужой** `rop_id` при создании конфига (по коду не должен — подставляется собственный id, но проверить обязательно).

> ⚠️ Все проверки выполнять **только на тестовом окружении**, с тестовым аккаунтом AmoCRM. Не трогать реальные подключения клиентов.

### ✅ Ответ команды

> **Пункт 1.** «Пока у руководителей нет доступа это сделать, но в будущем планируют реализовать».
>
> **Пункт 2.** «В UI этого нет, так что ROP не должен видеть свои ключи».

**Дата ответа:** 25.07.2026
**Кто ответил:** QA-заказчик (внутренняя постановка)
**Решение:** ROP **не должен** ни видеть ключи, ни управлять подключением. Управление планируется дать в будущем — отдельным решением.
**Дальше:** ☑ дефект прав доступа · ☑ чиним по образцу OnlinePBX · ☑ тест-кейс

### ⚠️ Ответ подтверждает дефект, а не снимает вопрос

Оба ответа описывают **интерфейс**: кнопки нет, экрана нет, значит доступа нет. Именно на этом расхождении и построен вопрос.

| Уровень | Что есть сейчас |
| --- | --- |
| Интерфейс (фронтенд) | Закрыт. Кнопка настройки видна только супер-админу — здесь ответ верен |
| Обращение напрямую к API | **Открыт.** По разбору кода ROP может выполнить `GET`, `PATCH`, `DELETE` над `/v1/amocrm/configs/{id}` — посмотреть ключи, сменить домен, удалить подключение |

Требование теперь зафиксировано однозначно: **ROP не должен видеть ключи и не должен управлять подключением.** Фактическое поведение этому противоречит. Значит это не «открытый вопрос», а **дефект безопасности** — кандидат **BUG-014**.

**Важность: 🔥 P1.** Обоснование:
- ключи AmoCRM дают полный доступ к CRM компании в обход нашей системы;
- ROP может подменить домен подключения — куда после этого уедут данные, не определено;
- ROP может удалить подключение и остановить работу всего отдела, супер-админ об этом не узнает;
- **это же поведение сейчас на production** — как и восемь июльских дефектов.

**Эталон для исправления уже есть в самом продукте.** Соседний модуль `/v1/onlinepbx/configs` закрыт правильно: явная проверка прав на каждом методе. Чинится по образцу, изобретать ничего не нужно.

**План проверки** — тот же, что описан выше (пункты 1–6). Ожидаемый результат теперь однозначен: `403 Forbidden` на всех методах для роли ROP. Любой `200 OK` — подтверждение дефекта. Выполняю на staging, с тестовым аккаунтом AmoCRM, как только он будет выдан.

### Что будет, когда управление всё-таки дадут ROP

Команда планирует выдать эту возможность в будущем. Тогда вернутся два вопроса, которые сейчас закрыты:
1. **показывать ли ключи.** Управление и видимость — разные вещи: можно разрешить переподключение через OAuth, не показывая сами ключи. Сегодняшний ответ — «не должен видеть» — стоит сохранить и после выдачи прав;
2. **журнал изменений.** Если подключением управляет не один человек, а несколько, нужна запись «кто и когда менял». Сейчас её нет.

Занёс в раздел «Отложено до реализации» в `07-questions-simple.md` — подниму, когда сообщат о выкладке.

### ✅ Пункт 3 отвечен 25.07: границы ролей действуют везде, включая API

> «Права автоматически выданы: админ, ROP и оператор. У админа другой UI и так далее. Если по API выдан доступ админа оператору — это баг».

**Это ровно тот ответ, который был нужен.** Вопрос «как именно устроена проверка — единым фильтром или вручную в каждом методе» оказался неважен: устройство — дело разработки. Важно требование, и оно теперь однозначное.

**Требование:** роль определяет границы доступа **на уровне API, а не только интерфейса**. Разный UI у ролей — следствие, а не сама защита. Любой доступ выше своей роли, полученный обращением напрямую, — дефект.

**Что это меняет в плане работ.** Раньше я закладывал ручной перебор методов под каждой ролью — недели работы с заведомыми пропусками. Теперь это формулируется как **матрица прав** и уходит в автотесты:

| | Супер-админ | ROP | Оператор |
| --- | --- | --- | --- |
| Каждый метод API | ожидаемый код ответа | ожидаемый код ответа | ожидаемый код ответа |

Автотест проходит по всем методам под каждой из трёх ролей и сверяет фактический код с ожидаемым. Один раз построенная матрица закрывает весь класс дефектов сразу и ловит новые двери при каждой выкладке — включая те, что появятся в будущих функциях.

**Это становится содержанием волны 1 автотестов** (вход, разграничение прав, изоляция данных между клиентами). Приоритет поднимаю: матрица прав важнее остальных проверок, потому что BUG-014 показывает, что дыры уже есть, а сколько их — неизвестно.

**BUG-014 подтверждается этим ответом прямо.** ROP через API получает доступ к подключению AmoCRM, которым по роли управлять не должен. По формулировке ответа это баг без оговорок.

**Дальше:** ☑ требование зафиксировано · ☑ матрица прав в волну 1 автотестов · ☑ BUG-014 подтверждён
**Что делаем дальше:** ☐ заводим баг · ☐ обновляем ТЗ · ☐ пишем тест-кейс на текущее поведение · ☐ снимаем вопрос

---

# Вопросы по жизненному циклу лида (Q-02 … Q-09)

> Все вопросы ниже возникли при сверке диаграммы `docs/lead-life-cycle.png` с кодом. Разбор — в `02-lead-status-glossary.md`, раздел 12.
>
> **Общая рамка вопросов:** диаграмма описывает более богатый жизненный цикл, чем реализован в коде. Возможны три объяснения: (1) сознательное упрощение для MVP, (2) функциональность запланирована на потом, (3) недоделка. **От ответа зависит, какое поведение QA считает эталонным** — сейчас на эти сценарии нельзя написать ни одного тест-кейса, потому что неизвестно, что считать правильным результатом.

---

<a id="q-02"></a>
## Q-02. Что должно происходить с лидом после исчерпания попыток дозвона?

**Кому:** продакт · **Важность:** высокая · **Статус:** 🟢 **Отвечен 24.07.2026** · **Связано с:** Д-01

### Что на диаграмме
От статуса «Ko'tarmadi» идёт ветка `4/4 muvaffaqiyatsiz` (4 из 4 неудачно) в отдельный статус **«Sifatsiz»** (некачественный лид). Есть отдельная сноска: *«Belgilangan retry muvaffaqiyatsiz bo'lsa, alohida statusga "Sifatsiz"»* — «если назначенные повторы неудачны, в отдельный статус "Sifatsiz"».

### Что в коде
Понятия «Sifatsiz» нет: ни класса статуса, ни перехода. Лид, исчерпавший лимит попыток (по умолчанию 4), **остаётся в статусе «Ko'tarmadi»** и просто перестаёт попадать в очередь обзвона. Никакого события, отметки или уведомления при этом не происходит.

Если статус с таким названием заведён у клиента в AmoCRM, он попадёт в класс «прочие» и тоже никогда не будет обзваниваться — но перевести в него лида может только человек вручную.

### Вопрос
1. **Какое поведение считается правильным** — автоматический перевод в «Sifatsiz» или сознательное решение оставить лид в «Ko'tarmadi»?
2. Если автоматический перевод нужен: откуда система должна узнать, **какой именно статус клиента** является «Sifatsiz» — по ключевому слову в названии (как остальные), или ROP должен указать его явно в настройках?
3. Должен ли ROP как-то **узнавать**, что лиды «отвалились»? Сейчас это происходит бесшумно: лид просто исчезает из работы, отчёта нет.

### Почему это важно для бизнеса и QA
«Sifatsiz» — это способ отделить мусорные заявки от рабочих. Без него в воронке накапливаются лиды в статусе «не взял трубку», которые внешне выглядят активными, но по факту мертвы. Руководитель не видит разницы между «ещё дозвонимся» и «уже никогда».

Для QA: пока нет ответа, невозможно проверить сценарий «что происходит после 4-й неудачной попытки» — неизвестен ожидаемый результат.

### ✅ Ответ команды

**Дата:** 24.07.2026 · **Источник:** `docs/MVP_SCOPE_UPDATED_2026-07-24.md` (решения продакта) + `docs/WORK_LOG_4.md` (реализация)

**Решение — по всем трём подвопросам:**

1. **Правильное поведение — автоматический перевод в «Sifatsiz».** Формулировка из скоупа: всего 4 попытки, неиспользованные переносятся на следующие дни, после исчерпания лид автоматически уходит в «Sifatsiz». Лид уходит из очереди и изменение отправляется в AmoCRM.
2. **Как система узнаёт, какой статус считать «Sifatsiz»** — по ключевому слову в названии, как и все остальные статусы. Добавлен отдельный класс, распознаваемый по слову `sifatsiz`. Явной настройки у РОП нет.
   **Предпосылка уже выполнена:** клиент завёл статус «Sifatsiz» во всех своих воронках AmoCRM — в скоупе это отмечено как закрытый пункт.
3. **Отдельно и важно:** AI **запрещено** выбирать «Sifatsiz» и «Yangi» как результат разговора — ни в промпте, ни в выпадающем списке оператора. Причина — реальный инцидент, когда AI пометил как «Sifatsiz» нормальный состоявшийся разговор. Этот статус теперь ставится **только** бизнес-правилом (4 недозвона) или человеком вручную.

**Что осталось без ответа:** третий подвопрос — должен ли РОП **узнавать**, что лиды отвалились. Отдельного отчёта или уведомления в решении нет. Лид просто меняет статус в AmoCRM, и увидеть это можно только там. Возможно, эту роль частично закроет утверждённая «карточка состояния интеграций», но напрямую это не заявлено. **Стоит переспросить.**

**Дальше:** ☑ тест-кейсы на новое поведение (см. `02-lead-status-glossary.md`, раздел 3.8) · ☑ проверка по коду V-01, V-02, V-03 · ☐ переспросить про видимость для РОП · ☑ баг-репорт **не нужен**

> ⚠️ **Оговорка:** ответ получен из документа, а не из разговора. Реализацию кодом мы не сверяли (папки `project/` нет). До сверки считаем поведение заявленным, а не подтверждённым.

---

<a id="q-03"></a>
## Q-03. Должен ли статус лида меняться автоматически, если AI не заполнил обязательные поля?

**Кому:** продакт · **Важность:** ~~высокая~~ → **средняя [снижена 25.07]** · **Статус:** 🟢 Отвечен 25.07 · **Связано с:** Д-02 (закрыто в пользу диаграммы)

> **[обновлено 25.07] Почему важность снижена.** В промпт AI заложили явную карту «исход разговора → статус», где случай «решение не принято» прямо отображается на «Chala gaplashdi». То есть типичную ситуацию с диаграммы AI теперь и так предложит правильно — по смыслу разговора. **[Ж]**
>
> Исходный вопрос остаётся: должен ли статус меняться **сам** именно по признаку незаполненных обязательных полей. Но практический разрыв между замыслом и реальностью сузился, и это перестало быть блокером для тест-кейсов.

### Что на диаграмме
После блока «Suhbat va AI tahlil» (разговор и AI-анализ) стоит развилка **`Majburiy fieldlar to'liqmi?`** («обязательные поля заполнены полностью?»). Ветка **«Yo'q»** (нет) ведёт в статус **«Chala gaplashdi»** («недоговорил»).

### Что в коде
Проверка заполненности есть, но она **не меняет статус лида**. Результат разговора помечается как `status = incomplete`, заполняется список `missing_required_fields`, и оператору в модалке подтверждения подсвечиваются незаполненные поля.

Какой статус будет у лида — решает **предложение AI и выбор оператора**, а не полнота полей.

### Вопрос
1. **Какое поведение правильное:** автоматически ставить «Chala gaplashdi» при незаполненных обязательных полях, или оставлять решение за оператором (текущее поведение)?
2. Если автоматически — **что важнее при конфликте?** Пример: AI уверенно определил статус «Uchrashuv vaqti» (клиент назначил встречу), но не смог заполнить обязательное поле «Filial». Ставим «Chala gaplashdi» и теряем информацию о встрече, или сохраняем статус AI и просто помечаем неполноту?
3. Может ли оператор подтвердить результат, **не заполнив** обязательные поля? (Сейчас — да, отправка не блокируется.)

### Почему это важно
Это развилка между «система страхует оператора» и «система доверяет оператору». От неё зависит целый пласт тест-кейсов: блокирующая валидация или предупреждающая.

Дополнительный риск: если отправка не блокируется, через 20 секунд бездействия сработает **автосинхронизация**, и в AmoCRM уедет заведомо неполная карточка — без участия человека.

### ✅ Ответ команды

> **Если обязательные поля не заполнены — статус переходит в «Chala gaplashdi».** Автоматически, по признаку неполноты формы.

**Дата:** 25.07.2026  **Кто ответил:** QA-заказчик (внутренняя постановка)  **Решение:** диаграмма верна, код — нет
**Дальше:** ☑ баг · ☑ доработка · ☐ обновляем диаграмму · ☑ тест-кейс

### Что из этого следует для QA

**Расхождение Д-02 закрывается в пользу диаграммы.** Развилка `Majburiy fieldlar to'liqmi?` → «Yo'q» → «Chala gaplashdi» — это требование, а не устаревший рисунок. Код ведёт себя иначе: помечает `status = incomplete`, заполняет `missing_required_fields`, подсвечивает поля оператору — но статус лида оставляет на усмотрение AI и оператора.

**Это кандидат в дефект BUG-010.** Завожу после воспроизведения на стенде — правило проекта: баг только после самостоятельного воспроизведения.

| Проверка | Ожидаемый результат | Если не так |
| --- | --- | --- |
| Оператор оставляет обязательное поле пустым и подтверждает | Лид уходит в «Chala gaplashdi» независимо от предложения AI | **Баг** — текущее поведение именно такое |
| AI уверенно определил «Uchrashuv vaqti», но «Filial» пустой | Всё равно «Chala gaplashdi». Неполнота важнее исхода разговора | **Баг** |
| Оператор ничего не нажал, сработала автоотправка через 20 секунд | Уходит со статусом «Chala gaplashdi» | Баг, если уезжает со статусом от AI |
| Все обязательные поля заполнены | Статус — тот, что предложил AI / выбрал оператор. Принудительного «Chala» нет | Баг обратного знака |

**Ответы на пункты 2 и 3 исходного вопроса выведены из этого же правила:**
- **пункт 2** — при конфликте побеждает неполнота: ставим «Chala gaplashdi», информация о встрече в статус не попадает. Само значение поля «время встречи», если AI его извлёк, при этом сохраняется — проверю отдельно, чтобы данные не терялись молча;
- **пункт 3** — отправка не блокируется, оператор может подтвердить форму с пустыми полями. Правило говорит не «запретить», а «поставить Chala». Значит и автоотправка через 20 секунд остаётся, но с этим статусом.

**Риск R-16 переоценён в меньшую сторону.** Пустая карточка всё так же уедет в AmoCRM без участия человека, но теперь она уедет с честным статусом «недоговорил» — то есть будет видна как незавершённая работа, а не потеряется среди нормальных. Из блокера это становится рабочим наблюдением.

---

<a id="q-04"></a>
## Q-04. Что должно происходить при переносе времени визита?

**Кому:** продакт · **Важность:** высокая · **Статус:** 🟢 **Отвечен 24.07.2026** · **Связано с:** Д-16

### Что на диаграмме
Отдельная сноска: *«Kelish vaqti o'zgarsa, unda bitta orqaga yani markazga kelish vaqtiga o'tadi, agar u ertangi yoki undan keyingi kunlar bo'lsa, agar bugun bo'lib vaqti o'zgarsa, eski flow qaytariladi»* — «если время прихода изменилось, лид откатывается на шаг назад, ко времени прихода в центр; если это завтра или позже — так; если сегодня и время изменилось — возвращается старый поток».

То есть предполагается **откат лида назад по цепочке статусов** при переносе договорённости.

### Что в коде
Логики отката нет. Поля `arrival_at` и `appointment_at` просто перезаписываются при следующей синхронизации из AmoCRM, статус при этом не меняется.

Побочный эффект: лид, уже переведённый автоматически в «Bugun keladi», при переносе визита на следующую неделю **останется в «Bugun keladi»** и продолжит выдаваться операторам как срочный.

### Вопрос
1. **Какое поведение правильное** при переносе времени визита — откат статуса назад или сохранение текущего?
2. Различается ли поведение, если новое время **сегодня** и если **в другой день**?
3. Кто инициирует перенос — оператор в разговоре, или изменение прилетает из AmoCRM (менеджер поправил карточку)? От этого зависит, где реализовывать логику.

### Почему это важно
Прямое влияние на работу операторов: лид с перенесённым визитом продолжает считаться «горячим» и занимать первое место в очереди. Оператор звонит человеку, который придёт только через неделю.

### ✅ Ответ команды

**Дата:** 24.07.2026 · **Источник:** `docs/MVP_SCOPE_UPDATED_2026-07-24.md`, раздел 6 — строка помечена «Approved (2026-07-24)», заведена отдельная задача в ClickUp

**Решение — правило подтверждено, поведение зависит от новой даты:**

| Новое время визита | Что должно произойти |
| --- | --- |
| Перенесено на **завтра или позже** | Лид возвращается назад в поток «Uchrashuv» (встреча назначена на будущее) |
| Перенесено **в рамках сегодняшнего дня** | Поток «Bugun keladi» запускается заново |

То есть замысел с диаграммы (Д-16) подтверждён: откат назад по цепочке — это требование, а не недоразумение.

**Статус реализации:** ❗ **не разработано.** В скоупе строка стоит как Open / Open. Задача заведена, срок неизвестен.

**Что осталось без ответа:** третий подвопрос — кто инициирует перенос: оператор в разговоре или изменение прилетает из AmoCRM от менеджера. От этого зависит, где искать точку входа при тестировании. **Переспросить.**

**Дальше:** ☑ тест-кейсы пишем заранее · ☑ применяем после релиза · ☑ диаграмма подтверждена, менять не нужно · ☐ уточнить, кто инициирует перенос

---

<a id="q-05"></a>
## Q-05. Что происходит с лидом после визита в центр?

**Кому:** продакт · **Важность:** средняя · **Статус:** 🟢 **Отвечен 24.07.2026** · **Связано с:** Д-03

### Что на диаграмме
От «Markazga keldi» (пришёл в центр) идут две ветки:
- **«To'ladi»** (оплатил) → «Yakuniy holat» (финальное состояние);
- **«To'lamadi»** (не оплатил) → «Keyingi qo'ng'iroq vaqti», со сноской *«AmoCRM da vaqt belgilanadi»* (время назначается в AmoCRM).

Есть и вторая сноска: *«To'lov amalga oshirmasa (Qo'lda o'zgartiriladi, field → to'lov qilgan qilmagani bilish uchun)»* — «если оплата не произведена (меняется вручную, поле → чтобы понять, оплатил или нет)».

### Что в коде
Автоматических переходов нет. «Markazga keldi» полностью выведен из очереди обзвана — дальнейшее движение лида возможно только руками в AmoCRM. Поля «оплатил / не оплатил» в логике системы не участвует.

### Вопрос
1. Судя по сноске *«Qo'lda o'zgartiriladi»* (меняется вручную), **это и задумано как ручная работа**? Если да — расхождения нет, нужно просто зафиксировать это в документации.
2. Должен ли Operator AI как-то участвовать в стадии «после визита», или его зона ответственности заканчивается на моменте, когда клиент дошёл до офиса?
3. Если клиент пришёл, но не оплатил — кто и когда возвращает его в обзвон?

### Почему это важно
Определяет **границу продукта**. Если зона ответственности заканчивается на «Markazga keldi» — это нужно явно записать, и тогда все ветки после визита из тест-плана исключаются.

### ✅ Ответ команды

**Дата:** 24.07.2026 · **Источник:** `docs/MVP_SCOPE_UPDATED_2026-07-24.md`, раздел 6 — «Paid / not-paid manual field», помечено «Approved (2026-07-24)», заведена задача в ClickUp

**Решение — граница продукта проведена, но чуть дальше, чем мы предполагали:**

1. **Да, это ручная работа** — как и подсказывала сноска на диаграмме («Qo'lda o'zgartiriladi»). Признак оплаты заполняет человек, система его не выводит из разговора.
2. **Но продукт в этой стадии всё же участвует:** добавляется отдельное поле «оплатил / не оплатил». То есть факт оплаты становится данными системы, а не только пометкой в AmoCRM.
3. **Кто возвращает неоплатившего клиента в обзвон:** сотрудник центра ставит время следующего звонка **в AmoCRM**. Дальше лид подхватывается обычным механизмом очереди — это уже знакомый нам сценарий «Keyingi qo'ng'iroq vaqti».

**Статус реализации:** ❗ **не разработано.** В скоупе строка стоит как Open / Open.

**Что это значит для тест-плана:** ветки после визита из плана **не исключаются**, но откладываются. Ключевой кейс на будущее: «пришёл, не оплатил → сотрудник поставил время в AmoCRM → лид вернулся в очередь в нужный момент».

**Дальше:** ☑ фиксируем как ручной процесс с поддержкой в системе · ☑ тест-кейсы пишем заранее · ☑ диаграмма подтверждена

---

<a id="q-06"></a>
## Q-06. Что такое «Yakuniy holat» и как система понимает, что работа с лидом закончена?

**Кому:** продакт · **Важность:** средняя · **Статус:** 🟡 Отвечен 25.07 (частично) · **Связано с:** Д-04 (закрыто)

> ### ✅ Ответ 25.07: да, это системные статусы 142/143
>
> «Yakuniy holat» на диаграмме — не отдельная сущность, а системные статусы AmoCRM `142` (успешно реализовано) и `143` (закрыто и не реализовано). Расхождение **Д-04 закрыто** как дефект наименования на диаграмме: код верен, термин на схеме неточен.
>
> **Что из этого следует для QA:**
>
> | Проверка | Ожидаемый результат | Если не так |
> | --- | --- | --- |
> | Перевести лид в `142` | Исключён из очереди, повторных звонков нет | Баг |
> | Перевести лид в `143` | То же самое | Баг |
> | Вернуть лид из `142` в рабочий статус вручную | Возвращается в обзвон | Баг (или осознанное ограничение — уточню при проверке) |
>
> **Главный приёмочный тест теперь формулируется.** Критерий «клиент прошёл весь путь от заявки до покупки» = лид дошёл до `142`. Это опора для сквозного автотеста, который раньше нельзя было написать из-за неопределённости термина.
>
> ### ✅ Пункт 3 отвечен 25.07: нет, обрабатываться одинаково они не должны
>
> **Лид в `143` («закрыто и не реализовано») должен возвращаться в очередь и обзваниваться, пока не дойдёт до `142`.** Отказ клиента — не конец работы, а промежуточный исход.
>
> **Это прямое расхождение с кодом.** Сейчас оба статуса исключаются из очереди одним условием. Кандидат в дефект **BUG-013**, завожу после воспроизведения на стенде.
>
> | Проверка | Ожидаемый результат | Если не так |
> | --- | --- | --- |
> | Перевести лид в `142` | Исключён из обзвона окончательно | Баг |
> | Перевести лид в `143` | **Возвращается в очередь**, обзвон продолжается | **Баг — BUG-013.** Сейчас исключается |
> | Лид прошёл `143` → снова обзвонили → довели до `142` | Теперь исключён | Баг |
>
> **Уточнение к формулировке «Yakuniy holat».** Финальным по смыслу остаётся только `142`. `143` — статус, который система знает, но точкой остановки он не является. Правка внесена в `02-lead-status-glossary.md`.
>
> **Риск R-18, который фиксирую сам, вопросом не делаю.** Правило «возвращать, пока не купит» без ограничителя означает бесконечный обзвон отказавшихся. Ограничитель в системе уже есть — 4 неудачных попытки → «Sifatsiz». Принимаю по умолчанию, что возврат из `143` подчиняется общим правилам очереди, а не идёт в обход них: счётчик попыток работает, «Sifatsiz» наступает. Если окажется, что возврат сбрасывает счётчик, получится вечный цикл — оформлю багом.
>
> **Дата:** 25.07.2026 · **Кто ответил:** QA-заказчик (внутренняя постановка) · **Дальше:** ☑ уточняем термин в документации · ☑ тест-кейсы · ☑ опора для сквозного автотеста · ☑ кандидат в баг

### Что на диаграмме
Присутствует отдельное состояние **«Yakuniy holat»** («финальное состояние»), в которое лид попадает после успешного прохождения цепочки.

### Что в коде
Такого понятия нет. Единственный признак «работа закончена» — **системные статусы AmoCRM**: `142` (успешно реализовано) и `143` (закрыто и не реализовано). Лиды в них исключаются из очереди жёстко прописанным условием.

### Вопрос
1. **«Yakuniy holat» на диаграмме — это и есть системные статусы AmoCRM 142/143?** Если да, расхождение снимается, нужно только уточнить термин в документации.
2. Если нет — какой конкретно статус имеется в виду и по какому признаку система должна его распознавать?
3. Правильно ли, что **оба** системных статуса (и «успешно», и «закрыто без результата») обрабатываются одинаково — просто исключением из обзвона?

### Почему это важно
«Финальное состояние» — это критерий завершения основного бизнес-сценария. Без точного определения нельзя написать приёмочный тест «лид успешно прошёл полный цикл».

### Ответ команды
> _(заполнить)_

**Дата:**  **Кто ответил:**  **Решение:**
**Дальше:** ☐ уточняем термин в документации · ☐ доработка · ☐ тест-кейс

---

<a id="q-07"></a>
## Q-07. Приемлемы ли ограничения автоматического перевода «Uchrashuv» → «Bugun keladi»?

**Кому:** продакт + разработчик BE · **Важность:** средняя · **Статус:** 🔴 Открыт · **Связано с:** Д-09

### Контекст
Этот переход работает в целом так, как на диаграмме (`Uchrashuv kuni 00:00` → «Bugun keladi»). Вопрос не в самом переходе, а в его технических ограничениях, которые на диаграмме не отражены.

> **[уточнено 25.07]** Раньше здесь было «единственный автоматический переход в системе». Теперь их **два**: рядом появился переход «Ko'tarmadi» → «Sifatsiz» **[Ж]**. Оба живут в одном фоновом процессе, но вызываются независимо.
>
> **Это расширяет вопрос:** все ограничения из таблицы ниже (задержка до 5 минут, лимит на проход, работа только для РОП с назначенными операторами) относятся к процессу целиком — а значит, действуют и на переход в «Sifatsiz». Уточнять надо сразу по обоим.

### Что в коде
| Ограничение | Значение | Последствие |
| --- | --- | --- |
| Периодичность проверки | раз в **5 минут**, жёстко в коде | Переход происходит не ровно в 00:00, а с задержкой до 5 минут |
| Лимит за один проход | **200 лидов** | При большем объёме остальные ждут следующего прохода |
| Область действия | только ROP, **у которых есть операторы, назначенные на воронки** | У остальных переход не работает вовсе |
| Если в воронке нет статуса со словом `bugun` | лид молча остаётся на месте | Ни ошибки, ни записи в лог |
| Если поле времени встречи не заполнено | переход не срабатывает | Молча |

### Вопрос
1. **Приемлемы ли эти ограничения** как поведение MVP, или что-то из списка нужно считать дефектом?
2. Достаточно ли лимита в 200 лидов за проход для ожидаемых объёмов клиентов? Сколько лидов у самого крупного клиента может иметь встречу на один день?
3. Должна ли система **сообщать**, что переход не сработал (нет статуса в воронке, не заполнено время)? Сейчас всё происходит бесшумно.

### Почему это важно для QA
Определяет допуски в тест-кейсах: проверять переход «сразу после полуночи» или «в течение 5 минут». Плюс задаёт необходимость нагрузочной проверки на 200+ лидов.

### ✅ Ответ команды (частично)

> **Пункт 1 — ограничения приемлемы.** Отдельно подтверждено ключевое: автоперевод **работает только там, где операторы назначены на воронки**. Это задумано, а не забыто.
>
> **Пункт 2 — лимита в 200 достаточно.** Обоснование: сколько оператор успеет обзвонить, столько и отработается. Пропускная способность упирается в людей, а не в размер пачки.
>
> **Пункт 3 — сигнал не нужен, вариант «а».** Обоснование: система переведёт автоматически. То есть отдельное уведомление незачем, потому что отказов быть не должно.
>
> ⚠️ **Это переворачивает трактовку.** Раньше «молча не сработало» читалось как допустимое поведение, о котором просто не сообщают. Теперь требование другое: **перевод обязан срабатывать**. Значит любой несработавший перевод — не тихая норма, а дефект.

**Дата:** 25.07.2026  **Кто ответил:** QA-заказчик (внутренняя постановка)  **Решение:** ограничения — поведение MVP, дефектом не считаем

### Что из этого следует для QA

| Проверка | Ожидаемый результат | Если не так |
| --- | --- | --- |
| Воронка **с** назначенными операторами, встреча на сегодня | Лид переведён в «Bugun keladi» в течение 5 минут после полуночи | Баг |
| Воронка **без** назначенных операторов | Лид **не** переводится — и это норма, не дефект | Баг обратного знака: если переводит, значит область действия шире описанной |
| То же самое для перехода «Ko'tarmadi» → «Sifatsiz» | Оба перехода живут в одном фоновом процессе → ограничение действует одинаково | Расхождение между двумя переходами — баг |
| Задержка до 5 минут | Допуск в тест-кейсах: «в течение 5 минут после полуночи», не «ровно в 00:00» | — |
| **301 лид со встречей на один день** | Первые 200 переведены сразу, остальные 101 — на следующем проходе, максимум через 5 минут. Ни один не потерян | **Баг**, если «хвост» не переводится вовсе |

**Нагрузочная проверка на 200+ снимается из плана** как отдельная задача: лимит признан достаточным по бизнес-обоснованию. Остаётся один функциональный кейс — что «хвост» за пределами первых 200 всё-таки доезжает следующим проходом, а не теряется. Это не про нагрузку, а про корректность обработки очереди.

**Важное следствие, которое фиксирую как риск R-17.** Раз область действия — осознанное ограничение, то у клиента, который завёл воронку и **не назначил на неё операторов**, автопереводы не работают вовсе: встречи не станут «Bugun keladi», недозвоны не уйдут в «Sifatsiz». Молча, без единого сообщения. Для клиента это выглядит как «система не работает», а формально всё по спецификации. Пересекается с пунктом 3, который остался открытым: именно такие ситуации и стоило бы показывать.

**Проверка приоритета:** это первое, что смотрю при разборе жалобы «у нас автопереводы не работают» — назначены ли операторы на воронку. Вношу в будущий чек-лист диагностики.

### Три случая молчаливого отказа — новая трактовка

| Случай | Было (до ответа) | Стало |
| --- | --- | --- |
| В воронке нет статуса со словом `bugun` | Допустимо, просто без уведомления | **Дефект.** Кандидат BUG-011: лид молча остаётся на месте, список «сегодня придут» пустой |
| Не заполнено время встречи | Допустимо | **Дефект.** Кандидат BUG-012. Как чинить — решать команде: либо не давать сохранить статус встречи без времени, либо подставлять значение по умолчанию. Я фиксирую расхождение с требованием, способ исправления не навязываю |
| На воронку не назначены операторы | Осознанное ограничение (ответ 25.07) | **Остаётся ограничением.** См. противоречие ниже |

### ⚠️ Противоречие между двумя ответами одного дня

- по пункту 1: автоперевод **работает только там, где назначены операторы** — это задумано;
- по пункту 3: система **переведёт автоматически**, поэтому сигнал не нужен.

Для воронки без операторов эти два ответа несовместимы: по первому перевода не будет и это норма, по второму перевод обязан произойти.

**Как разрешаю по умолчанию:** побеждает первый ответ — он конкретнее и касался именно этого случая. Воронка без операторов остаётся мёртвой зоной по спецификации, риск R-17 в силе. Случаи 1 и 2 при этом считаю дефектами.

Если трактовка неверна — поправляется одной строкой, переписывать ничего не придётся.
**Дальше:** ☐ приемлемо, фиксируем допуски · ☐ баг · ☐ доработка

---

<a id="q-08"></a>
## Q-08. Каким должно быть поведение кнопки «пропустить лида»?

**Кому:** продакт · **Важность:** средняя · **Статус:** 🔴 Открыт · **Связано с:** Д-13

### Контекст
В коде есть действие, которого **нет на диаграмме**: оператор может пропустить выданного лида, не звоня ему.

### Что в коде
- Лид откладывается на **30 минут** (значение жёстко в коде, ROP настроить не может);
- попытка дозвона **не засчитывается** — счётчик `attempts_count` не растёт;
- блокировка снимается, лид возвращается в общий пул;
- количество пропусков **ничем не ограничено**.

### Вопрос
1. **Задумано ли действие «пропустить»**, или оно появилось как техническое решение?
2. Должен ли ROP видеть, **как часто операторы пропускают лидов**? Сейчас это никак не отражается в отчётности.
3. Должно ли быть ограничение на число пропусков одного лида? Сейчас лид можно пропускать бесконечно — он будет возвращаться каждые 30 минут и снова пропускаться.
4. Должен ли интервал в 30 минут быть настраиваемым (как остальные параметры в «Mezonlar»)?

### Почему это важно
Потенциальный способ для оператора «не работать»: неудобные лиды можно пропускать весь день, и в отчётах это никак не проявится — попытки не засчитываются.

### ✅ Ответ команды

> **Первая версия ответа (25.07, утро):** кнопку «пропустить» уберут.
>
> **Уточнение (25.07, позже) — план изменён.** Кнопку **оставляют**, но вводят ограничение: оператор сможет пропустить **2 или 3 раза за рабочий день**. Точное число не зафиксировано. Реализация — «в будущем», срок не назван.

**Дата:** 25.07.2026  **Кто ответил:** QA-заказчик (внутренняя постановка)  **Решение:** не удаление, а лимит пропусков на рабочий день
**Дальше:** ☐ поведение верное · ☑ доработка (лимит) · ☑ добавить в отчётность · ☑ тест-кейс

### Что из этого следует для QA

**Пункты 2 и 3 исходного вопроса возвращаются в работу.** Я снимал их с формулировкой «незачем дорабатывать то, что удаляют» — это больше не так. Раз функция остаётся и обрастает правилом, нужны и счётчик, и его видимость.

| Проверка (после реализации) | Ожидаемый результат |
| --- | --- |
| Оператор пропускает лида N раз за день (N = лимит) | Каждый пропуск отложил лида на 30 минут, попытка не засчитана |
| Попытка пропустить сверх лимита | Отказ. Как именно — кнопка неактивна или сообщение — пока не определено |
| Начался новый рабочий день | Счётчик пропусков обнулился |
| Оператор закрыл и снова открыл смену в тот же день | Лимит **не** обнуляется — иначе ограничение обходится в один клик |

Последняя строка — моё допущение, не подтверждённое требование. Формулировка «за один подход рабочего дня» допускает оба прочтения: лимит на сутки или лимит на смену. Разница принципиальная: если счётчик привязан к смене, оператор обходит правило переоткрытием смены. **Вынес это отдельным вопросом**, потому что от прочтения зависит не допуск в тесте, а само наличие дыры.

**Пункт V-20 переформулирован.** Было: «убедиться, что кнопка исчезла и метод закрыт». Стало: «убедиться, что лимит проверяется **на сервере**, а не только гашением кнопки в интерфейсе». Ограничение, живущее лишь во фронтенде, обходится обращением напрямую — это ровно та же схема, что в Q-01.

**Риск R-9 остаётся в силе и стал долгосрочным.** До реализации лимита пропуски по-прежнему не засчитываются как попытки и не видны в отчётах, а число их не ограничено. Раньше я рассчитывал, что риск закроется удалением кнопки в ближайшей выкладке; теперь он живёт до появления лимита, срок которого не назван.

---

<a id="q-09"></a>
## Q-09. Требование ли — звонить клиентам только с определённого номера?

**Кому:** продакт · **Важность:** низкая · **Статус:** 🟢 **Отвечен 24.07.2026** · **Связано с:** Д-17, Д-07

### Что на диаграмме
Отдельный блок с номерами: `Mijoz nomeri 99 800 03 34` → `78 113 71 61`, со сноской **«78 113 71 61 faqat shu orqali qilinsin»** — «пусть делается только через этот номер». Второй вариант ветки: `2 99 800 03 34` → `55 055 9889`.

### Что в коде
Логики принудительного исходящего номера не найдено. Номер, с которого идёт звонок, определяется настройками OnlinePBX на стороне клиента, а не Operator AI.

### Вопрос
1. **Это требование к продукту** (система должна принудительно звонить с конкретного номера) или просто пример настройки телефонии конкретного клиента, зафиксированный на диаграмме «на память»?
2. Если требование — где оно должно настраиваться: в Operator AI или на стороне OnlinePBX?
3. Есть ли правило выбора номера в зависимости от номера клиента (диаграмма показывает две пары)?

### Почему это важно
Если это требование — нужен тест «звонок уходит с правильного номера», а сейчас проверять нечего. Если это заметка про настройку телефонии — вопрос снимается, и достаточно дописать пояснение на диаграмму.

### ✅ Ответ команды

**Дата:** 24.07.2026 · **Источник:** `docs/MVP_SCOPE_UPDATED_2026-07-24.md`, раздел 6 — строка «Auto second phone number», решение «Removed (2026-07-24)»

**Решение:** **это не требование к продукту.** Номер `78 113 71 61` — это **номер самой телефонии OnlinePBX**, то есть номер, с которого система звонит. На диаграмме он оказался рядом с номером клиента и выглядел как «второй номер лида», хотя таковым не является.

**Неожиданное последствие — важнее самого ответа.** Это разъяснение вскрыло ошибку в уже работающей функции: в системе был сделан **автоперебор второго номера контакта AmoCRM**. Но «второй номер» в карточке контакта — это и есть номер OnlinePBX. То есть система при неответе клиента начинала звонить **сама себе**.

Решение: функцию автоперебора **удалить**, набирать только собственный номер лида.

**Что это меняет у нас:**

| Документ | Что делать |
| --- | --- |
| `02-lead-status-glossary.md`, Д-07 | Обновлено: требование снято |
| `04-call-flow.md`, шаг 3 (ветка А) | Обновлено: описание перебора номеров помечено как удаляемое |
| План тестирования | Кейсы «клиент не ответил → звоним на второй номер» — **убрать** |
| Проверка | V-17 в `06-scope-status.md`: убедиться, что перебор действительно удалён |

**Дальше:** ☑ снимаем вопрос · ☑ уточняем диаграмму (подписать, что 78 113 71 61 — номер телефонии) · ☑ убираем кейсы из плана

---

<a id="q-10"></a>
# Q-10. Контракт с AmoCRM: где зафиксированы ключевые слова статусов и названия полей, и кто отвечает за их неизменность?

**Кому:** продакт + разработчик BE + тот, кто занимается подключением клиентов (внедрение)
**Важность:** 🔥🔥 критическая — влияет на работоспособность продукта у каждого клиента
**Статус:** 🔴 Открыт · **Связано с:** Д-05, разделы 2 и 7 файла `02-lead-status-glossary.md`

> ### 🔺 [обновлено 25.07] Вопрос стал ещё важнее — контракт расширился
>
> Раньше «договор по названиям» касался двух вещей: ключевых слов в статусах и двух полей с датами. Теперь к ним добавилось третье, и оно крупнее обоих. **[Ж] [Р]**
>
> **Форма подтверждения звонка больше не настраивается РОП вручную — она собирается автоматически из группы полей AmoCRM, в названии которой есть слово «operator».** Решение от 24.07: РОП не строит ничего.
>
> Это удобно, но означает, что **вся форма целиком теперь зависит от того, как клиент назвал группу полей в своей CRM**. Переименовал группу — форма не собралась. Плюс уже известен реальный сбой этого механизма: одна и та же группа хранится отдельной копией для каждой воронки, и система привязывалась не к той копии — из-за чего поле «Markazga kelish vaqti» какое-то время просто не отображалось. Решили брать копию с наибольшим числом полей.
>
> **Три новых вопроса к списку ниже:**
>
> 1. Зафиксировано ли где-то требование «группа полей в AmoCRM должна называться так, чтобы в названии было слово *operator*»?
> 2. Что должно происходить, если группы **нет вообще** или она названа иначе? Сейчас, судя по всему, форма просто не соберётся — молча, как и всё остальное.
> 3. Правило «берём копию с наибольшим числом полей» — это осознанное решение или обходной путь? Если у двух воронок разный набор полей, оператор одной из них увидит лишние или недостающие поля.
>
> **Для QA это добавляет целый блок негативных тест-кейсов** — см. `06-scope-status.md`, раздел 6.1, пункт 4.

## Суть простыми словами

Operator AI не хранит собственный справочник статусов и полей. Он **читает названия, которые клиент придумал у себя в AmoCRM, и пытается угадать их смысл по словам внутри названия**.

Аналогия: представьте переводчика, который не знает языка, но выучил 7 слов. Пока собеседник использует эти слова — всё работает. Стоит сказать то же самое другими словами — переводчик молча кивает и ничего не переводит.

Проблема в том, что **этот список слов нигде не зафиксирован как договорённость с клиентом**. Он существует только внутри кода, в одном файле.

## Что именно зашито в код

### 1. Ключевые слова статусов
Файл: `project/back/internal/usecase/calling/status_class.go`, функция `classifyStatusName`. Поиск **по вхождению** в название (регистр не важен, апострофы игнорируются):

| Ключевое слово | Во что превращается | Что будет, если слова нет |
| --- | --- | --- |
| `kotarmadi` | Не взял трубку | лид не обзванивается |
| `chala` | Недоговорили | лид не обзванивается |
| `keyingi` + `qongiroq` (оба) | Перезвонить в назначенное время | лид не обзванивается |
| `bugun` | Придёт сегодня | лид не обзванивается |
| `uchrashuv` | Встреча назначена | не будет автоперевода в «Bugun keladi» |
| `markaz` | Пришёл в центр | — |
| `yangi` | Новый | лид не обзванивается |

### 2. Названия полей с датами
Файл: `project/back/internal/usecase/amocrm/service.go:1892-1893`. Здесь совпадение **точное**, а не по вхождению:

| Название поля в AmoCRM | Для чего используется | Что будет при другом названии |
| --- | --- | --- |
| **`call vaqti`** | Время визита клиента → «звонить за час до» | Правило «за час до визита» отключается, лид становится доступен просто с начала рабочего дня |
| **`uchrashuv vaqti`** | Дата встречи → автоперевод в «Bugun keladi» | Автоперевод не срабатывает вообще |

## Почему это критично

**Всё ломается молча.** Ни одного сообщения об ошибке, ни записи в логе, ни предупреждения в интерфейсе. Со стороны ROP это выглядит так: *«у операторов пустая очередь»* или *«лиды почему-то не звонятся»* — без единой подсказки о причине.

**Сломать может кто угодно и когда угодно.** Менеджер клиента переименовал этап воронки в AmoCRM с «Ko'tarmadi» на «Недозвон» — и в этот момент обзвон этой категории лидов остановился навсегда. В AmoCRM это выглядит как безобидная косметическая правка.

**Есть ловушки на ровном месте:**
- «Markazga **kelmadi**» (*не* пришёл) содержит `markaz` → система считает, что клиент пришёл;
- «Yangi lid — bugun keladi» будет распознан как «Bugun keladi», потому что `bugun` проверяется раньше `yangi`;
- поле «Kelish vaqti» вместо «call vaqti» — не найдено, хотя по смыслу это одно и то же.

## Вопросы

1. **Где зафиксирован этот контракт для клиента?** Есть ли документ, чек-лист внедрения или инструкция, где написано: «ваши статусы в AmoCRM обязаны содержать такие-то слова, поля должны называться ровно так»? Или это устная договорённость при подключении?
2. **Кто отвечает за то, что клиент не переименует статус или поле?** Внедрение при подключении, ROP по инструкции, или никто?
3. **Должна ли система проверять контракт и предупреждать?** Например, при выборе воронки показывать ROP: *«В воронке не найден статус для категории "не взял трубку" — такие лиды не будут обзваниваться»*. Сейчас экран правил показывает статусы, но никак не сообщает, что часть из них система не понимает.
4. **Правильна ли сама модель угадывания по подстроке?** Альтернатива — явное сопоставление: ROP на экране правил вручную указывает, какой его статус соответствует какой роли в системе («вот этот мой статус = не взял трубку»). Это снимает всю категорию проблем разом. Планируется ли такое?
5. **Почему для полей точное совпадение, а для статусов — по вхождению?** Это осознанное решение или так сложилось?
6. **Есть ли эталонная воронка**, которую разворачивают новому клиенту при подключении? Если да — можно ли получить её описание? Это лучшая основа для тестовых данных.

## Влияние на QA

Пока нет ответа:
- **невозможно подготовить корректные тестовые данные** — неизвестно, какую воронку считать «правильно настроенной»;
- **невозможно оценить долю риска у реальных клиентов** — нельзя сказать, у скольких сейчас статусы названы «неправильно»;
- нельзя решить, писать ли автотесты на негативные сценарии распознавания (ложные срабатывания вроде «Markazga kelmadi»).

Если контракт будет зафиксирован, из него сразу вырастает:
- чек-лист приёмки нового клиента («воронка настроена корректно»);
- набор автотестов на распознавание статусов (позитивные + ловушки);
- раздел в пользовательской документации для ROP: «что нельзя переименовывать в AmoCRM и почему».

## ✅ Ответ команды

> **Документ-контракт не нужен, отдельный ответственный не нужен.** В нашу систему приходят те же статусы, что заведены в AmoCRM — своего списка статусов мы не ведём. Если клиент переименует статус, у нас он тоже переименуется: синхронизация с AmoCRM идёт **каждые 2 минуты**.
>
> **Прямое указание от 25.07:** вопрос закрыт. Ожидаемое поведение задано — переименование статуса в AmoCRM не должно ломать работу системы. **Если после переименования обзвон по этой категории встаёт — это баг, а не открытый вопрос.**

**Дата:** 25.07.2026  **Кто ответил:** QA-заказчик (внутренняя постановка)  **Решение:** контракт с клиентом не оформляем, полагаемся на синхронизацию
**Дальше:** ☑ должно работать → воспроизводим и заводим баг · ☐ пишем чек-лист приёмки клиента

### Что из этого следует для QA

Требование зафиксировано, значит вопрос уходит из списка открытых и превращается в **проверку с известным ожидаемым результатом**:

| Проверка | Ожидаемый результат | Если не так |
| --- | --- | --- |
| В тестовом AmoCRM переименовать статус `Ko'tarmadi` → `Недозвон`, подождать 2 минуты | Лиды этой категории продолжают попадать в очередь обзвона | **Баг**, важность 🔥🔥 — тихая остановка обзвона |
| Переименовать поле «call vaqti» | Время следующего звонка продолжает записываться | **Баг** |
| Завести статус-ловушку «Markazga kelmadi» | Лид **не** распознаётся как «пришёл в центр» | **Баг** |
| Убрать или переименовать группу полей «operator» | Форма подтверждения пустая, **никакой реакции системы**: ни сообщения, ни блокировки подключения | Баг, если вместо пустой формы падает ошибка или белый экран |

**Дополнение к ответу (25.07):** отсутствие группы полей «operator» — не аварийная ситуация. Ожидаемое поведение: форма собирается пустой, и на этом всё. Отдельного сообщения оператору, запрета на подключение воронки и запасной ручной настройки не предусмотрено.

**Риск R-16, вытекающий из этого.** Пустая форма не отменяет автоотправки в CRM через 20 секунд бездействия — значит, у клиента без нужной группы полей карточки будут уходить в AmoCRM пустыми, без участия оператора. Проверить связку «пустая форма → автоотправка» (пункт V-21) и, если подтвердится, вынести предложение: при пустой форме таймер автоотправки не запускать. Пересекается с Q-03.

**Блокер проверки:** нужен тестовый аккаунт AmoCRM — по Q-15 он ещё не подтверждён. Пока доступа нет, проверка стоит в очереди, а не считается пройденной.

**Расхождение, которое надо снять кодом (V-19):** по журналам разработчиков распознавание идёт **поиском ключевого слова в названии** — при таком устройстве переименование ломает привязку, и ожидаемое поведение из ответа не выполняется. Если же система хранит внутренний идентификатор статуса AmoCRM, всё сходится. Пометка [Ж] — кодом не проверено, `project/` в рабочей директории нет.

---

<a id="q-11"></a>
# Q-11. Каков статус двух параметров «Mezonlar», которые сохраняются, но не влияют на очередь?

**Кому:** продакт + разработчик BE
**Важность:** высокая · **Статус:** 🔴 Открыт · **Связано с:** Д-11, Д-12 (файл `02-lead-status-glossary.md`, раздел 12)

> **Важно:** это наблюдение **по чтению кода**, на живом стенде пока не воспроизводилось. Вопрос задаётся именно как уточнение статуса функциональности, а не как заявка о дефекте — вполне возможно, что работа над этими параметрами ещё не завершена.

> ### 🔄 [обновлено 25.07] Вопрос по `work_end` изменил формулировку
>
> В журнале работ за 17–22 июля появилась задача: **повторная попытка, выпавшая на нерабочее время, переносится на утро следующего рабочего дня** — «ночью оператор не звонит». **[Ж]**
>
> Значит, конец рабочего дня где-то учитывается, и наше исходное утверждение «параметр не используется вообще» больше не точно. Но остаётся принципиальная развилка, и **тест-кейсы для этих двух вариантов разные**:
>
> | Вариант | Что означает | Как проверять |
> | --- | --- | --- |
> | **А. Ограничивается только планирование повторов** | Правило касается лишь статуса «Ko'tarmadi»: время следующей попытки не назначается на ночь | Ночью очередь **по-прежнему выдаёт** лидов в статусах «Yangi lid» и «Bugun keladi» — и это корректно |
> | **Б. Ограничивается вся выдача** | После `work_end` очередь не выдаёт лидов вообще | Ночью очередь пуста в любом статусе |
>
> **Что нужно от команды:** какой из двух вариантов является требованием. Плюс отдельно — статус `default_call_time` (по нему нового ничего не появилось, вопрос ниже актуален полностью).
>
> Проверка по коду — пункты V-05 и V-06 в `06-scope-status.md`. **Баг-репорт по `work_end` пока не заводим** — до сверки с кодом.

## Суть

На экране «Mezonlar» ROP настраивает 8 параметров очереди обзвона. Шесть из них участвуют в логике выбора лидов. Два — **сохраняются в базу и возвращаются в интерфейс, но в правилах очереди не используются**.

## Что именно

### 1. `work_end` — конец рабочего дня (по умолчанию 20:00)

**Ожидаемое поведение по смыслу параметра:** после указанного часа очередь перестаёт выдавать лидов операторам.

**Что видно в коде:** в SQL-запросе выборки лидов (`ClaimNextLeadForOperator`, `db/query/queries/calling.sql`) участвует только `work_start` — как момент, с которого становятся доступны лиды «Bugun keladi» без указанного времени визита. Условия, ограничивающего выдачу сверху, в запросе нет.

Параметр валидируется при сохранении (`settings.go:152` — проверяется, что конец позже начала), сохраняется в `rop_call_settings.work_end`, отдаётся в ответе `GET /v1/calling/settings` в блоке `effective`. В формировании правил очереди (`queueRulesForROP`, `queue_rules.go`) не участвует.

**Следствие, если поведение именно такое:** ROP выставляет конец рабочего дня 18:00, но в 23:00 очередь продолжает выдавать лидов на обзвон.

### 2. `default_call_time` — время звонка по умолчанию (по умолчанию 09:00)

**Замысел виден из комментария в самом коде** (`internal/usecase/calling/settings.go:24`):
```go
defaultDefaultCallTime = "09:00" // day-only agreements ("dushanba deng") call at this hour
```
То есть: клиент сказал «перезвоните в понедельник», не назвав часа → звоним в понедельник в 09:00.

**Что видно в коде:** значение сохраняется, резолвится в `EffectiveCallSettings.DefaultCallTime`, отдаётся в API. Поиск по всему бэкенду показывает, что за пределами файла настроек и слоя хранения оно **нигде не читается** — ни в правилах очереди, ни при планировании времени следующего звонка.

**Следствие, если поведение именно такое:** договорённость «в понедельник» без указания часа не превращается в конкретное время звонка. При этом для статуса «Keyingi qo'ng'iroq vaqti» время перезвона **обязательно** — без него лид выпадает из очереди (см. `02-lead-status-glossary.md`, раздел 3.2). То есть параметр задумывался как страховка ровно от этой ситуации.

## Вопросы

1. **Каков статус этих двух параметров** — функциональность в разработке, отложена, или отменена, а поля остались от предыдущей версии замысла?
2. Если работа **не завершена** — стоит ли пока скрыть их на экране «Mezonlar»? Сейчас ROP видит настройки, меняет их и ожидает эффекта.
3. Если функциональность **отменена** — правильно ли будет удалить параметры из интерфейса и API, чтобы не вводить в заблуждение?
4. По `work_end` отдельно: **должна ли очередь вообще ограничиваться рабочим временем?** Возможно, замысел был другим — например, ограничивать не выдачу лидов, а что-то ещё (учёт смен, отчётность).
5. По `default_call_time` отдельно: **как сейчас должен поступать оператор**, если клиент назвал день без времени? Ставить час самому, или для этого есть другое соглашение?

## Влияние на QA

Пока статус не определён, эти два параметра **исключены из тест-плана** — неизвестно, что считать правильным результатом. После ответа возможны три исхода:

- «в разработке» → тест-кейсы пишем заранее, применяем после релиза;
- «отменено» → проверяем, что параметры убраны из интерфейса;
- «должно работать» → **воспроизводим на стенде** и, если подтвердится, заводим баг-репорты.

**План воспроизведения на стенде** (выполнить до заведения багов):
1. Под ROP выставить `work_end` = текущее время минус 1 час, сохранить;
2. Убедиться, что в базе значение сохранилось, а `GET /v1/calling/settings` возвращает его в блоке `effective`;
3. Под оператором этого ROP запросить `GET /v1/calling/queue` и `GET /v1/calling/next`;
4. Зафиксировать: выдаётся ли лид после окончания рабочего дня;
5. Для `default_call_time`: подтвердить звонок со статусом «Keyingi qo'ng'iroq vaqti`, **не передавая** время следующего звонка; проверить в `lead_call_states`, заполнилось ли `next_call_at` значением из настройки или осталось пустым.

> Только на тестовом окружении.

## ✅ Ответ команды (частично)

> **`work_end_time` — вариант Б, подтверждён 25.07.** После 20:00 в очереди для операторов не остаётся ни одного лида. Тест-кейс финализируем.
>
> **`default_call_time` — уже реализовано.** Целевое поведение сформулировано так: клиент сказал «позвоните в понедельник», часа не назвал → в понедельник, в начале рабочего дня, этот лид должен **высвечиваться первым** в очереди оператора.

**Дата:** 25.07.2026  **Кто ответил:** QA-заказчик (внутренняя постановка)  **Решение:** Б подтверждён; `default_call_time` реализован, поведение задано
**Дальше:** ☐ в разработке — ждём · ☐ отменено, убираем из интерфейса · ☑ должно работать → воспроизводим и заводим баг · ☐ поведение верное, уточняем смысл параметров

> ⚠️ **Расхождение с моим наблюдением.** По разбору кода от 21–23.07 параметр `default_call_time` нигде не читался. Ответ говорит «уже сделано» — вероятнее всего, реализация появилась в более свежей версии, чем та, что я смотрел. Пометку [К] по этому пункту снимаю до перепроверки. Это ещё один довод за то, чтобы подтвердить, какая версия выложена на стенд: проверять правило на старой сборке бессмысленно.

### Что из этого следует для QA

| Проверка | Ожидаемый результат | Если не так |
| --- | --- | --- |
| Запросить `GET /v1/calling/queue` и `/next` в 20:30 | Пусто | **Баг** |
| То же в 19:30 | Лиды выдаются | Баг (обратная сторона: не отрезать рабочее время) |

**Три допущения, принятые без отдельного вопроса** (расхождение оформляю багом, а не уточнением):
1. правило симметрично — до `work_start_time` очередь тоже пуста;
2. время считается по часовому поясу компании-клиента, не сервера;
3. звонок, начатый в 19:59, доводится до конца, и форма подтверждения работает после 20:00.
| Подтвердить звонок со статусом «Keyingi qo'ng'iroq vaqti», указав **день без времени** (понедельник) | `next_call_at` = понедельник, время из `default_call_time`; поле не пустое | **Баг** — при пустом `next_call_at` лид выпадает из обзвона навсегда |
| В понедельник в начале рабочего дня запросить очередь | Этот лид — **первый** в выдаче, не просто присутствует в списке | **Баг** приоритизации |
| Изменить `default_call_time` с 09:00 на другое значение | Лид назначается на новое время | Баг — настройка декоративная |

**Проверяю сразу после подтверждения деплоя на стенд.** Порядок в очереди («первым») проверяю отдельно от факта назначения — это два разных механизма, и сломаться может любой из них по отдельности.

---

<a id="q-12"></a>
# Q-12. Новый модуль тарифов: ограничивает ли тариф доступ к функциям?

**Кому:** продакт + разработчик BE
**Важность:** высокая — это потенциально механизм разграничения доступа, а значит зона тестирования прав
**Статус:** 🔴 Открыт · **Дата обнаружения:** 23.07.2026 (появилось в коммитах 21–22.07)

## Что добавили

В систему добавили модуль **тарифных планов**. Их ровно три, с фиксированными кодами:
- `taxlil_dashboard` — «аналитический дашборд»;
- `taxlil_dashboard_auto_call` — «дашборд + автообзвон»;
- `full_ai` — «полный AI».

Что появилось в коде:
- таблица `plans` и три плана;
- публичный `GET /v1/plans` — используется на лендинге в блоке цен;
- `PUT /v1/plans/{code}` — **только super_admin**, редактирует лишь витрину (название, цену, описание, список фич);
- у пользователя (ROP) появилось поле `users.tariff`;
- у заявок с лендинга появились поля `tariff` и `converted_at` (когда заявку сконвертировали в клиента);
- на лендинге в форме демо-заявки появился **необязательный** выбор тарифа;
- в админке — экран `/dashboard/plans` для super_admin.

## В чём вопрос

По коду видно, что тариф **сейчас нигде не ограничивает функциональность**. Поиск показал: `users.tariff` записывается при создании ROP и хранится, но **ни один модуль** (очередь звонков, AI, AmoCRM, OnlinePBX) не проверяет тариф перед тем, как дать доступ к функции. То есть на данный момент тариф — это:
- витрина цен на лендинге;
- атрибут клиента (просто записан);
- то, что редактирует super_admin.

Но сами названия планов намекают на разграничение: `taxlil_dashboard` (только дашборд) vs `taxlil_dashboard_auto_call` (+ автообзвон) vs `full_ai` (всё). Это выглядит как **будущий механизм ограничения доступа**.

## Вопросы

1. **Должен ли тариф ограничивать доступ к функциям?** Например: ROP на тарифе `taxlil_dashboard` **не должен** иметь автообзвона, а на `full_ai` — должен всё.
2. Если да — **это ещё не реализовано** (проверок в коде нет). Тогда: это в разработке, и нам готовить тест-кейсы заранее? Какой тариф что открывает — есть матрица «тариф × функция»?
3. Если нет (тариф — только для биллинга и маркетинга) — тогда он не входит в тестирование прав, и мы просто фиксируем это.
4. **Кто и когда назначает тариф ROP?** При создании super_admin'ом? Меняется ли потом?
5. **Контроль enum на лендинге.** Значение тарифа в форме демо-заявки должно **точно** совпадать с backend-enum (в коде это прямо прокомментировано). Кто следит, что значения на лендинге и на бэкенде не разойдутся? (тот же класс проблемы, что и Q-10 — контракт по строковым значениям).

## Влияние на QA

- Если тариф **будет** гейтить функции — это раздел тестирования прав доступа, сопоставимый по важности с ролями. Понадобится матрица «тариф × функция × роль», и это кандидат на автоматизацию.
- Если **нет** — вычёркиваем из тест-плана, оставляем только смоук: тариф сохраняется, отображается, редактируется super_admin'ом.
- В обоих случаях: проверить, что `PUT /v1/plans/{code}` **недоступен** роли ROP и оператору (по коду — только super_admin, но проверить, как Q-01).

## ✅ Ответ команды

> **Пункт 1 — да, тариф ограничивает доступ.** «У каждого тарифа свои условия».
>
> **Пункт 2 — уже реализовано.**
>
> Пункты 4 и 5 (кто назначает тариф; контроль enum на лендинге) — ответа пока нет.

**Дата:** 25.07.2026  **Кто ответил:** QA-заказчик (внутренняя постановка)  **Решение:** тариф гейтит функции, механизм реализован
**Дальше:** ☑ тариф гейтит функции → готовим матрицу и тесты · ☐ тариф только биллинг → смоук · ☑ пишем тест на доступ к PUT /plans

### ⚠️ Второе расхождение с моим разбором кода

По коду от 21–23.07 проверок тарифа **не было ни в одном модуле**. Ответ говорит «реализовано». Это тот же случай, что с `default_call_time`: скорее всего, я смотрел версию старше текущей. Пометку [К] с этого пункта снимаю до перепроверки.

**Два расхождения подряд по одной причине** — это уже не случайность. Подтверждение версии на стенде становится блокирующим условием для всей проверки: на старой сборке я получу ложные дефекты по обеим темам.

### Матрицу вывожу сам из названий тарифов

Готовой таблицы «тариф × функция» мне не передали, но названия планов её задают достаточно однозначно. Беру за рабочую гипотезу, проверяю на стенде, расхождения оформляю:

| Тариф | Аналитика и отчёты | Автообзвон | AI-анализ звонков |
| --- | --- | --- | --- |
| `taxlil_dashboard` | ✅ | ❌ | ❌ |
| `taxlil_dashboard_auto_call` | ✅ | ✅ | ❌ |
| `full_ai` | ✅ | ✅ | ✅ |

Если фактическое поведение разойдётся с этой таблицей — не завожу баг сразу, а сначала показываю таблицу команде: возможно, границы проведены иначе. Это тот случай, где моя гипотеза слабее их знания.

### Что это делает с планом автотестов

Проверка прав становится **двумерной**: доступ определяется и ролью, и тарифом. Матрица волны 1 растёт из «метод × роль» в «метод × роль × тариф» — вместо трёх колонок девять.

Это заметно увеличивает объём, но не меняет подхода: матрица строится один раз и дальше работает сама. Ручной перебор при таком размере невозможен в принципе.

**Отдельные проверки, которые добавляю:**

| Проверка | Ожидаемый результат |
| --- | --- |
| Клиент на `taxlil_dashboard` обращается к автообзвону напрямую, минуя интерфейс | Отказ. Скрытая кнопка при живом методе — это BUG-014 в миниатюре |
| Понижение тарифа у клиента с активной сменой оператора | Определить фактическое поведение: обрывается ли работа, доживает ли смена до конца. В требованиях не описано |
| `PUT /v1/plans/{code}` под ролями ROP и оператор | Отказ — менять тарифы может только супер-админ |

Последняя строка связана с долгом по проду: 23.07 при воспроизведении BUG-008 я менял тарифы на боевой системе. Уборку делаю до начала проверок тарифов, иначе буду тестировать по испорченным данным.

---

<a id="q-13"></a>
# Q-13. Какой документ скоупа считать действующим, и где два недостающих документа решений?

**Кому:** продакт
**Важность:** высокая — от этого зависит, относительно чего мы вообще проверяем систему
**Статус:** 🟢 **Отвечен 25.07.2026 (частично)** · **Дата обнаружения:** 25.07.2026

> ### ✅ Ответ получен 25.07: действует новый скоуп
>
> **Работаем по `MVP_SCOPE_UPDATED_2026-07-24.md`.** Старый `MVP-Scope.md` для оценки готовности не используется.
>
> Это подтверждает рабочее правило, которое мы уже применили при обновлении документации: расхождение Р-07 закрыто как дефект документа, карта готовности берётся из нового скоупа (`06-scope-status.md`, раздел 2).
>
> **Что осталось открытым:**
>
> | Пункт | Статус |
> | --- | --- |
> | Какой документ действует | ✅ Новый, от 24.07 |
> | Вывести старый скоуп из обращения | ⏳ Не сделано. Два противоречащих документа по-прежнему лежат рядом. Минимально достаточно приписки в шапке `MVP-Scope.md`: «устарело, см. документ от 24.07» |
> | Передать `SAVOLLAR_VA_QARORLAR_2026-07-24.md` и `SCOPE_DECISIONS.md` | ⏳ Не переданы. Без них часть решений читается двояко — прямо влияет на Q-14 |
> | Регулярная передача журналов работ | ⏳ Договорённости нет |
>
> Вопрос остаётся в статусе «отвечен частично» до закрытия трёх нижних строк.

## Суть

В проекте сейчас **два документа скоупа, которые противоречат друг другу**:

| Документ | Дата | Что говорит |
| --- | --- | --- |
| `docs/MVP-Scope.md` | старый | Весь раздел «AI-guided calling» — Open / Open, то есть «не разработано» |
| `docs/MVP_SCOPE_UPDATED_2026-07-24.md` | 24.07.2026 | Тот же раздел — почти целиком Done, плюс решения продакта |

Новый документ помечен как **черновик на согласование** и явно оговаривает: *«does NOT replace MVP_SCOPE.md yet»* — то есть официально старый документ всё ещё действует.

Практически это означает: если завтра кто-то откроет `MVP-Scope.md`, он сделает вывод, что ядро продукта не разработано. Мы уже наступили на это — первая редакция `01-project-analysis.md` фиксировала это как крупнейшее расхождение (Р-07).

## Второе: двух документов у нас нет

Новый скоуп ссылается на два файла, которые нам не передали:

1. **`SAVOLLAR_VA_QARORLAR_2026-07-24.md`** — вопросы клиенту и разработчикам плюс принятые решения, с нумерацией (в скоупе встречаются ссылки вида «client Q1.2», «dev Q2.10»);
2. **обновлённый `SCOPE_DECISIONS.md`**.

У нас есть только краткие пересказы решений в ячейках таблиц. Для формулировок вроде «неиспользованные попытки переносятся на следующие дни» этого **недостаточно** — см. Q-14.

## Вопросы

1. **Какой документ считать действующим прямо сейчас?** Мы опираемся на новый — правильно ли это?
2. **Когда черновик станет утверждённым скоупом**, и заменит ли он старый файл физически? Два противоречащих документа в одной папке — это ловушка для любого нового человека в команде.
3. **Можно ли получить `SAVOLLAR_VA_QARORLAR_2026-07-24.md` и `SCOPE_DECISIONS.md`?**
4. Есть ли договорённость, что журнал работ передаётся QA **регулярно**, а не по случаю? За две недели, о которых мы узнали задним числом, три функции успели появиться и попасть в список на удаление.

## Влияние на QA

~~Пока не ясно, какой документ действует, невозможно сформулировать критерий приёмки.~~ **[закрыто 25.07]** Критерий приёмки берётся из документа от 24.07.

Остаточный риск — организационный: старый документ никуда не делся и лежит рядом. Для нас это уже не проблема, для следующего человека в проекте — будет.

## Ответ команды

**Дата:** 25.07.2026 · **Решение:** действует новый документ от 24.07

**Дальше:** ☑ определён действующий документ · ☐ получили недостающие документы решений · ☐ договорились о регулярной передаче журналов · ☐ старый скоуп выведен из обращения

---

<a id="q-14"></a>
# Q-14. Политика попыток: что означает «переносятся на следующие дни» и сбрасывается ли счётчик при ручной правке статуса?

**Кому:** продакт + разработчик BE
**Важность:** высокая — это правило напрямую задаёт, когда лид уходит из работы
**Статус:** 🔴 Открыт · **Дата обнаружения:** 25.07.2026 · **Связано с:** Q-02, раздел 8.1 в `02-lead-status-glossary.md`

## Суть

Решение от 24.07 звучит так: **всего 4 попытки; неиспользованные попытки переносятся на следующие дни; после исчерпания — автоматически «Sifatsiz»**.

Первая и третья части понятны. **Вторая допускает два прочтения**, и они дают разное поведение:

| Прочтение | Что это значит | Пример |
| --- | --- | --- |
| **А. Суточного лимита просто нет** | 4 попытки на весь срок жизни лида, когда бы они ни случились | Сегодня дозванивались дважды — завтра доступны оставшиеся две |
| **Б. Есть дневная норма с накоплением** | В день положено N попыток; неизрасходованные складываются | Сегодня из 2 положенных сделали 1 — завтра доступно 3 |

По нашему разбору кода от 21.07 реализовано прочтение **А**: счётчик пожизненный, поле с датой попыток существует, но в правилах очереди не участвует. Прочтение **Б** в коде не встречалось.

Скорее всего, формулировка просто неудачная и имелось в виду А — но **на этом строится тест-кейс**, гадать нельзя.

## Второе: открытый пункт, который команда зафиксировала сама

В скоупе есть строка со статусом **TBD**: *сброс счётчика попыток при ручном изменении статуса человеком*. То есть решения ещё нет.

По коду на 21.07: счётчик сбрасывается в ноль при **любой** смене статуса в AmoCRM. Отсюда следствие, которое стоит проговорить с продактом:

> Менеджер в AmoCRM переставил статус лида и вернул обратно — бюджет попыток обнулился. Лид, который уже был признан безнадёжным, снова попадает в обзвон. Сделать это можно неограниченно.

Это может быть и полезной функцией («дали лиду второй шанс»), и дырой в правиле четырёх попыток. Зависит от того, чего хочет бизнес.

## Вопросы

1. **Какое прочтение верное — А или Б?**
2. Считается ли попыткой звонок, на который **клиент ответил**? По коду счётчик растёт при **любом** подтверждении, независимо от того, взяли трубку или нет. Для правила «4 недозвона → Sifatsiz» логичнее было бы считать только неудачные.
3. **Должен ли счётчик сбрасываться**, когда статус меняет человек? Если да — не открывает ли это возможность бесконечно возвращать в работу отбракованные лиды?
4. Должен ли сброс работать одинаково при смене статуса **в AmoCRM** и **в нашей модалке подтверждения**?

## Влияние на QA

Пункт 2 особенно важен: если попыткой считается любой звонок, то лид, с которым **четыре раза нормально поговорили**, тоже уйдёт в «Sifatsiz» — а это уже похоже на дефект, а не на правило. Проверить это надо в первую очередь (пункт V-04).

## ✅ Ответ команды (частично)

> **Пункт 1 — прочтение А.** Дневного лимита нет: 4 попытки на всё время. Позвонили сегодня дважды — завтра осталось две.
>
> **Пункт 2 — успешный разговор попыткой не считается.** Если разговор состоялся, счётчик **сбрасывается обратно на 4** (значение по умолчанию).
>
> **Пункт 3 — да, так и должно быть.** Если статус меняют вручную в AmoCRM, это изменение приходит к нам при синхронизации и применяется. Возврат отбракованного лида в работу — не дыра в правиле, а штатный способ дать второй шанс; ограничения на число таких возвратов не предусмотрено.
>
> **Пункт 4** отдельно не оговаривался — принимаю, что поведение одинаковое, источник изменения роли не играет. Проверю на стенде, спрашивать не буду.

**Дата:** 25.07.2026  **Кто ответил:** QA-заказчик (внутренняя постановка)  **Решение:** прочтение А; состоявшийся разговор обнуляет счётчик; ручная правка статуса в AmoCRM тоже
**Дальше:** ☑ уточнили формулировку · ☑ решение по сбросу принято · ☑ тест-кейсы

### Что из этого следует для QA

| Проверка | Ожидаемый результат | Если не так |
| --- | --- | --- |
| 2 недозвона сегодня → посмотреть остаток завтра | Осталось 2 попытки, дневная норма не начисляется | Баг |
| Состоявшийся разговор, лид остался в обзвоне | Счётчик снова равен 4 | Баг |
| 4 состоявшихся разговора подряд | Лид **не** уходит в «Sifatsiz» | **Баг** — это был кандидат № 1 в дефекты |
| Лид в «Sifatsiz» → вручную сменить статус в AmoCRM → подождать синхронизацию | Лид вернулся в очередь, счётчик обнулён | Баг |
| То же самое, но статус меняется в нашей модалке подтверждения | Поведение идентично правке в AmoCRM | Баг (расхождение источников) |

**Риск, вытекающий из ответа (R-15):** если счётчик сбрасывается от **любого** отвеченного звонка, лид, который берёт трубку и сразу кладёт, не отбракуется никогда — правило четырёх попыток на нём не сработает. Не вопрос, а наблюдение: проверю на стенде, как определяется «состоявшийся разговор» (по факту ответа или по длительности), и если отбраковка не наступает — заведу баг.

---

<a id="q-15"></a>
# Q-15. Доступ к тестовому стенду и сроки закрытия внешних блокеров

**Кому:** разработчики + заказчик
**Важность:** 🔥🔥 критическая — **это блокировало всю практическую работу QA**
**Статус:** 🟢 **Отвечен 25.07.2026 (частично)** · **Дата обнаружения:** 25.07.2026 · **Связано с:** `06-scope-status.md`, раздел 4

> ### ✅ Ответ получен 25.07: доступ есть
>
> **Доступ выдан и к staging, и к production.** Главный блокер снят — можно переходить от чтения кода к выполнению проверок и написанию автотестов.
>
> **Правило работы с окружениями (не меняется):**
>
> | Окружение | Что разрешено |
> | --- | --- |
> | **staging** | Всё: создание, изменение, удаление данных, прогон автотестов |
> | **production** | **Только пассивное наблюдение.** Открыть страницу, посмотреть. Никаких изменений данных |
>
> Доступ к проду — это удобство для разбора инцидентов и сверки поведения, а **не** разрешение там тестировать. Правило проекта в силе.
>
> **Реквизиты доступа в документацию не пишем** — только в `.env`, который в `.gitignore`. Это касается адресов окружений, логинов и токенов.
>
> ### Что осталось открытым
>
> | Пункт | Статус |
> | --- | --- |
> | Доступ к staging и production | ✅ Есть |
> | Ротация ключа Gemini | ✅ Выполнена (BUG-009 → «Исправлен») |
> | Тестовый аккаунт AmoCRM | ⏳ Не подтверждён |
> | Тестовый аккаунт OnlinePBX | ⏳ Не подтверждён |
> | Доступ к логам стенда | ⏳ Не подтверждён. Без них разбор молчаливых отказов из `04-call-flow.md` крайне затруднён |
> | Эталонная воронка AmoCRM | ⏳ Не подтверждена — связано с Q-10 |
> | **Блокер: лимит расходов Gemini** | ⏳ Открыт. Отрезает всю ветку AI |
> | **Блокер: деплой бэкенда на стенды** | ⏳ Не подтверждён. Без него на стенде нет ни «Sifatsiz», ни блока «Причина звонка» |
> | **Блокер: внутренний номер оператора в OnlinePBX** | ⏳ Открыт. Блокирует живой исходящий звонок, то есть сквозной сценарий целиком |
> | Уведомления о деплоях на стенд | ⏳ Договорённости нет |
>
> ### ⚠️ Что это значит для планирования
>
> Доступ есть, но **три внешних блокера остаются**. Практически это означает: сценарии, не требующие живого звонка и AI (права доступа, изоляция данных, CRUD, настройки, правила очереди на уровне API), можно проверять **уже сейчас**. Сквозной сценарий «позвонили → расшифровали → подтвердили → уехало в CRM» — **пока нет**.
>
> Первоочередная задача: **уточнить, выкачен ли на стенд свежий бэкенд.** От этого зависит, что вообще имеет смысл там проверять — половина нового функционала может просто отсутствовать.

## Суть

Из журналов стало понятно две вещи.

**Первая — хорошая: staging существует.** Разработчики читают его логи, деплоят туда, подтверждают восстановление сервиса. Раньше мы этого не знали и в `05-bugs.md` писали «если staging-окружения нет — это повод поднять вопрос». **[закрыто 25.07]** Стенд есть, и доступ к нему теперь получен — вместе с доступом к production.

**Вторая — тяжёлая: пять внешних шагов блокируют проверку.** Это не задачи разработки, их закрывают заказчик и DevOps:

| № | Блокер | Что заблокировано |
| --- | --- | --- |
| 1 | Ключ Gemini скомпрометирован, нужна ротация | Безопасность (заведено как BUG-009) |
| 2 | Превышен месячный лимит расходов Gemini | **Вся ветка AI:** расшифровка, автозаполнение формы, резюме |
| 3 | Бэкенд не выкачен на стенды | Всё новое: «Sifatsiz», блок «Причина звонка», починка синхронизации |
| 4 | `AMOCRM_WEBHOOK_URL` не задан; адрес тестовой админки не в списке разрешённых источников | Обновления в реальном времени; вход в админку со стенда |
| 5 | Внутренний номер оператора **выключен** в панели OnlinePBX | **Любой живой исходящий звонок** |

Блокер 5 самый болезненный: без него невозможно проверить **сквозной сценарий целиком** — ядро продукта. Блокер 2 отрезает вторую половину сценария.

## Вопросы

1. **Доступ к staging:** адрес админки, учётные записи для трёх ролей, тестовый аккаунт AmoCRM, тестовый аккаунт OnlinePBX. Кто выдаёт?
2. **Кто ответственный по каждому блокеру и какой ожидаемый срок?** Особенно по пункту 5 — включению внутреннего номера.
3. **Можно ли получить доступ к логам стенда** (в журналах упоминается Dozzle)? Без логов разбор «почему звонок не дошёл» превращается в гадание — а по нашей карте отказов в `04-call-flow.md` большинство отказов **молчаливые**.
4. **Есть ли отдельная тестовая воронка в AmoCRM** с правильно названными статусами и полями? Это напрямую связано с Q-10: без эталонной воронки нельзя подготовить корректные тестовые данные.
5. **Как узнавать о деплоях на стенд?** Автотесты будут падать на несовпадении версий, если мы не знаем, что выкатили.

## Влияние на QA

~~Пока доступа нет, мы можем только читать код и документацию.~~ **[закрыто 25.07]** Доступ получен — проверки и автотесты возможны.

Что остаётся справедливым: баги BUG-001…BUG-008 пришлось искать на боевой системе с изменением реальных данных, потому что другого окружения тогда не было. **Повторять это нельзя** — теперь и не нужно. Плюс за нами долг: убрать с прода следы тех проверок.

## Ответ команды

**Дата:** 25.07.2026 · **Решение:** доступ выдан к staging и production; ключ Gemini ротирован

**Дальше:** ☑ доступ получен · ☑ ключ ротирован · ☐ сроки по оставшимся блокерам известны · ☐ подтверждён деплой свежего бэкенда на стенд · ☐ тестовая воронка AmoCRM есть · ☐ настроено уведомление о деплоях · ☐ убраны следы июльских проверок с прода

---

<a id="q-16"></a>
# Q-16. Автозакрытие смены по бездействию — чему равно N минут?

**Кому:** продакт
**Важность:** низкая
**Статус:** 🔴 Открыт · **Дата обнаружения:** 25.07.2026

## Суть

Сейчас забытая смена оператора закрывается автоматически в конце суток (23:59:59 по Ташкенту). Решением от 24.07 это меняется: смена должна закрываться **через N минут без звонков**.

Значение N в скоупе **не указано** — оно помечено как вопрос к разработчикам.

## Вопросы

1. **Чему равно N?** Без числа тест-кейс написать нельзя.
2. Что считается «активностью»: только завершённый звонок, или любое действие в интерфейсе? Оператор может час разбирать сложного клиента в CRM, не звоня — его смена не должна закрыться.
3. Что происходит с оператором, у которого смену закрыли автоматически, а он продолжает работать? Смена откроется заново сама или он должен нажать кнопку?
4. Как это соотносится с **перерывом**? Перерыв — это тоже отсутствие звонков.

## Влияние на QA

Затрагивает отчёт по рабочему времени и экран «Davomat» у РОП. Если N окажется маленьким, а перерывы не учитываются, отчёты будут врать — оператор ушёл на обед, вернулся, а смена закрыта.

## ✅ Ответ команды

> **Механизм меняется: вместо автозакрытия смены будет автопауза.** Если оператор не работает, смена уходит на паузу, а не закрывается. Реализация — в будущем; команда сообщит, когда выложат.

**Дата:** 25.07.2026  **Кто ответил:** QA-заказчик (внутренняя постановка)  **Решение:** автопауза вместо автозакрытия, детали приедут с реализацией
**Дальше:** ☐ N известно · ☐ определено, что считается активностью · ☑ тест-кейс заготовлен

### Что из этого следует для QA

**Вопрос снимается с повестки до выкладки.** Спрашивать N сейчас бессмысленно: механизм ещё не построен, и число будет частью реализации, а не отдельным решением.

**Смена механизма — к лучшему для пользователя.** Пауза обратима, закрытие — нет. Оператор, который отвлёкся на сложного клиента в CRM, теперь не теряет смену: он снимает паузу и работает дальше. Это снимает остроту с пунктов 2 и 3 исходного вопроса.

**Что уточню, когда сообщат о выкладке:**
1. через сколько минут без звонков наступает пауза;
2. что считается активностью — только завершённый звонок или любое действие в системе;
3. **как автопауза соотносится с ручным перерывом.** Это главное: два механизма паузы в одном экране — типовое место для путаницы. Что будет, если автопауза наступит во время обеда? Снимется ли она сама, когда оператор вернётся, или наложится на ручную;
4. возобновляется ли смена автоматически при первом действии оператора, или паузу надо снимать вручную.

**Влияние на отчёт «Davomat» переоценено в меньшую сторону.** Раньше риск был в том, что смена закроется и время потеряется. Пауза, скорее всего, будет видна в отчёте как отдельный интервал — но проверить это можно будет только на реализации. Тест-кейс заготовлю заранее: сумма «работа + пауза + перерыв» должна сходиться с фактической длительностью смены.

**До реализации** действует старое поведение: забытая смена закрывается в конце суток (23:59:59 по Ташкенту).

---

# Приложение. Вопросы, которые команда уже задала сама

**[добавлено 25.07]** В скоупе от 24.07 есть собственный список открытых вопросов команды — к клиенту и к разработчикам. Записываем его здесь, чтобы **не дублировать работу** и чтобы отследить ответы: почти каждый из них влияет и на тестирование.

Полные формулировки — в документе `SAVOLLAR_VA_QARORLAR_2026-07-24.md`, которого у нас нет (см. Q-13).

## Вопросы к клиенту

| Вопрос команды | Почему это важно для QA |
| --- | --- |
| Бывают ли лиды **без телефона**? | Прямо связано с инцидентом: лид без контакта обрывал всю синхронизацию. Нужен тест-кейс на такой лид |
| Действительно ли нужна **мульти-воронка** на оператора? | Если нет — упрощается матрица прав и тестовые данные |
| Продолжают ли менеджеры **править лиды в AmoCRM вручную**? | Это ровно наш риск R-3 (потеря данных при синхронизации). От ответа зависит, нужен ли механизм разрешения конфликтов |
| Какое **время звонка** запрашивают клиенты вне рабочих часов? | Связано с Q-11 и параметром `work_end` |
| Что делать, если клиент со статусом «Bugun keladi» **не подтвердил** визит по телефону? | Дыра в жизненном цикле, которой нет ни на диаграмме, ни в коде |

## Вопросы к разработчикам

| Вопрос команды | Почему это важно для QA |
| --- | --- |
| Существует ли правило **автоперевода в «Chala»**? | Это наш Q-03 — команда пришла к тому же вопросу |
| Соответствует ли код решению об **отмене автостатуса при недозвоне**? | Проверка V-18 |
| Детали **модели общего пула** | Влияет на тест гонки за лида (риск R-7) |
| Что показывать при **пустой очереди** после удаления сообщения | Меняет ожидаемый результат в шаге 1 |
| Ограничение на **медленный AI** | Связано с таймаутом ожидания в 3 минуты |
| **Порядок деплоя** | Критично при приёмке релиза: лендинг раньше бэкенда = потерянные заявки |
| **OAuth через Marketplace AmoCRM** | Новый способ подключения — потребует отдельных тест-кейсов |
| Удалены ли **второй номер** и **сообщение о пустой очереди** | Наши проверки V-16, V-17 |
| Закрытие смены по бездействию — **сколько минут** | Наш Q-16 |
| Задан ли **`AMOCRM_WEBHOOK_URL`** | Наш блокер № 4 |

**Вывод:** список вопросов команды и наш список пересекаются примерно наполовину. Это хороший знак — значит, мы смотрим в те же болевые точки. Имеет смысл предложить вести **один общий список вопросов**, а не два параллельных.
