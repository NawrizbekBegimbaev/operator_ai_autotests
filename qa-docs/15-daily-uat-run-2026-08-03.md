# Первый технический прогон ежедневного UAT — 03.08.2026

**Стенд:** staging из локального `.env`
**Каталог результата:** `uat-results/20260803-131440`
**Команда:** `.venv/bin/python scripts/run_daily_uat.py --telegram never`

## Результат

> Пройдено: **12 из 27 проверяемых**
> Ещё **6 заблокировано дефектом**; всего в чеклисте: **33**
> Время pytest: **3 мин 24 сек**

| Роль | Пройдено | Проверяемо | Всего |
| --- | ---: | ---: | ---: |
| Система | 2 | 2 | 2 |
| Общий путь ролей | 2 | 2 | 2 |
| Landing | 0 | 2 | 2 |
| Super-admin | 2 | 6 | 6 |
| ROP | 3 | 6 | 8 |
| Operator | 3 | 9 | 13 |

В реализованной и обеспеченной данными части продуктовых падений нет:
12 сценариев прошли. Общий статус остаётся красным, потому что 15 проверяемых
сценариев не были выполнены; они не засчитаны как зелёные.

## Не выполнено из-за внешних ворот

- Landing: нет `OPERATOR_AI_LANDING_BASE_URL` и удаляемого demo-канала.
- AmoCRM/OnlinePBX: нет выделенных UAT credentials и подтверждённого cleanup.
- Тарифы, назначения, подсказки и критерии: нет разрешения менять общие staging-данные.
- Очередь, AI/autosync, skip и три бизнес-перехода: нет детерминированного
  setup удаляемых лидов и call session.

Всего таких сценариев: 15. Для каждого отчёт содержит конкретное условие
активации; pytest skip не считается успехом.

## Заблокировано дефектом

Шесть сценариев выключены строгим сторожем из-за BUG-014, 017, 024, 026, 027,
029, 038 и 045. Основная цепочка «лид → звонок → AI → AmoCRM» остаётся среди
них, поэтому текущий результат ещё не отвечает «весь продукт работает».

## Что обнаружено во время запуска

На staging у ROP есть активный раздел «Не пришли» (`/dashboard/not-arrived`),
которого нет в доступном локальном Admin-коде. Раздел добавлен в
`UAT-ROP-001`; повторная проверка входа и шести разделов ROP прошла.

Первый полный прогон показал два ранних UI-падения во время незавершённой
загрузки страницы. Ожидания заменены на условие исчезновения progressbar и
готовность целевого элемента; повторный прогон этих путей и итоговый запуск
зелёные. Проверки не удалялись и не ослаблялись.

## Telegram

Генерация и `dry-run` доставки проверены. Позже бот был подключён к группе
`QA Results`, а отдельное тестовое сообщение успешно доставлено. Для GitHub
Actions токен хранится только как Repository Secret и не записывается в git.
